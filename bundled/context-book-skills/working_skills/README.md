# Working Skills

These skills are intended to be usable by an agent during normal Context Book operation.

- `context-book`: bootstrap/connect/init/complete helper plus deterministic REST readback for contexts, votes, and agents
- `context-book-discovery`: endpoint discovery helper with manual override precedence and mDNS/DNS-SD selection rules

These are operational helpers, not the primary public contract for external agent implementation.
